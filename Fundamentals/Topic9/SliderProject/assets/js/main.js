$(document).ready(function () {
  $(".slider_product").slick({
    autoplay: true,
    autoplaySpeed: 1000,
    // centerMode: true,
    dots: true,
    slidesToShow: 4,
    prevArrow: $(".custom-prev"),
    nextArrow: $(".custom-next"),
    arrows: true,

    // prev arrow
  });

  //   window.IntlPhone($("#phone"),function(){

  //   })
});
