// //? 1. Array empty
// //? 2. Prompt question for loop length
// //? 3. You able to create "for loop" for invite question.
// //? 4. We will fill array for log.

// var people = [];
// var inviteCount = ""; //promt

// //! for

// //! for

// assert.strictEqual(makeUpperCase(""), "");
// assert.strictEqual(makeUpperCase("hello"), "HELLO");
// assert.strictEqual(makeUpperCase("Hello"), "HELLO");
// assert.strictEqual(makeUpperCase("HELLO"), "HELLO");

function makeUpperCase(str) {
  // Code here
  var newStr = str.toUpperCase();

  return newStr;
}

console.log(makeUpperCase("hello"));
console.log(makeUpperCase("Hello"));
console.log(makeUpperCase("HELLO"));
console.log(makeUpperCase(""));
